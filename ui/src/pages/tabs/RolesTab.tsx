import { type Address } from "viem";
import { Card } from "../../components/Card";

export function RolesTab({ account }: { account: Address }) {
  return <Card title="Roles">Coming soon for {account}.</Card>;
}
